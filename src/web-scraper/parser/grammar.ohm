Refltag {
  Expr = fetch content? end

  fetch = "fetch" space+ url

  content = "\n" any+

  url =  nonWhitespace+
  
  space :=  ~"\n" whitespace // Inline whitespace
  nonWhitespace = ~whitespace any
  whitespace = " " | "\t" | "\n" | "\r"
}
